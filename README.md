## Text Analyzer — Лингвистический анализатор текста на русском языке
(Отличное описание, кратко и по делу. Вы молодец!)

Анализатор текста для русского языка, ориентированный на извлечение **грамматических и прагматических признаков**:
- Повелительные глаголы (императивы)
- Имена собственные (люди)
- Типы вопросов (общие / специальные)

Результат возвращается в виде структурированного списка по предложениям с точными позициями (start, end) — подходит для дальнейшей разметки, визуализации или обучения моделей.

## Структура проекта

(Не указано, что находится в папке `assets`. 
Здесь же напишу, что `word lists` и `interrogative words.txt` некорректные названия объектов в python-проекте - пробелы недопустимы. 
К тому же, вам необязательно было создавать дополнительную вложенность - можно было хранить `interrogative words.txt` напрямую в `assets` или убрать уровень `assets` и оставить только папку `word lists`.)

(Это не python)
```python
.
├── src/
│   └── api/  # api приложение  # Не нужно выносить этот файл в модуль (папка с `__init__.py`). Достаточно было положить его в корень `src`)
|   └── text_analyzer/  # основная логика анализа текста
│── tests/  # тесты

├── .gitignore
├── .pre-commit-config.yaml
├── poetry.lock
├── pyproject.toml  # конфигурация проекта и зависимостей (Poetry)  # Раз уж пишете про Poetry, то напишите и про dev-инструменты - Ruff, MyPy, Pre-Commit, PyTest. Они же тоже настраиваются в `pyproject.toml`
└── README.md
```

## Запуск

0) Создание виртуального окружения

(Некорректно. Раз вы используете Poetry, то и окружение нужно создавать с помощью него.)
```shell
py -m venv .env
.env\Scripts\activate
```

1) Установка зависимостей

```shell
poetry install
```

2) Запуск API

(Если бы вы не убрали `app.py` в модуль `api`, то это делать было бы не нужно.)

(Команда в целом неправильная. Нужно было `cd src/api`)
```shell
 cd .\src\api\
```

(А здесь была бы команда `poetry run uvicorn src.app:app`)
```shell
poetry run uvicorn app:app
```

API будет доступно по адресу: http://127.0.0.1:8000

Swagger UI: http://127.0.0.1:8000/docs

Эндпоинты API:

(Корневой эндпоинт можно не описывать в README. А вот к вашему `POST/analyze` слеовало бы добавить, что это **основной** эндпоинт.)
1) GET/ — Welcome endpoint (возвращает {"message": "Speech Analyzer API"})
2) POST/analyze — text analyzing (возвращает результаты анализа текста: найденные императивы, имена собственные и вопросы с указанием их типа)

## Пример ввода и вывода

### Ввод

POST /analyze
```
"Анна, подготовь отчёт к пятнице и отправь его Сергею Ивановичу! Иван Петрович, вы уже обсудили план проекта с командой?"
```
(Хорошо было бы добавить еще пример самого post-запроса.)

### Вывод

(НАЧАЛО КОММЕНТАРИЯ  
Вывод тоже следовало продемонстрировать именно такой, какой получил бы пользователь. 
У вас тут и типы данных другие, и ключи json-а не совпадают. Реальный ответ такой:
```
{
  "result": [
    {
      "id": 1,
      "text": "Анна, подготовь отчёт к пятнице и отправь его Сергею Ивановичу!",
      "persons": {"Анна": [0, 4], ...
```
КОНЕЦ КОММЕНТАРИЯ)

(Это не python)
```python
[
  {
    1: "Анна, подготовь отчёт к пятнице и отправь его Сергею Ивановичу!",
    "persons": {"Анна": (0, 4), "Сергею Ивановичу": (46, 62)},
    "is_question": {"general question": False, "specific question": False},
    "imperatives": {"отправь": (34, 41)}
  },
  {
    2: "Иван Петрович, вы уже обсудили план проекта с командой?",
    "persons": {"Иван Петрович": (0, 13)},
    "is_question": {"general question": True, "specific question": False},
    "imperatives": {}
  }
]
```


(Я так и не поняла, когда и как использовать пакет `text_analyzer`. Предполагаю, что пакет вам все-таки не нужен.)